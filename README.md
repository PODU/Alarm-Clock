# Alarm-Clock
Sleep-cycle alarm for Android. Pick how long to sleep, not when to wake — the app computes wake time from 90-minute cycles. Dismissing requires solving math, so you're actually awake. Quick Settings tile, home-screen widget, wake-up history with streaks. Kotlin + Jetpack Compose, no dependencies beyond AndroidX.
